<div class="container">
  <div class="row">
    <div class="col-md-8">
    <%= render partial: 'spree/shared/error_messages', locals: { target: @user } %>
      <div id="registration" data-hook>

        <div id="account">
          <%= render template: 'spree/user_sessions/new' %>
        </div>
      </div>
    </div>

        <div class="col-md-4">
          <div class="card login-card-style">
            <div class="card-block">
              <% if Spree::Config[:allow_guest_checkout] %>
              <div id="guest_checkout" data-hook>
                <h6 class="text-center login-helpers"><%= Spree.t(:guest_user_account) %></h6>
                <% if flash[:registration_error] %>
                  <div class='flash error'><%= flash[:registration_error] %></div>
                <% end %>
                <%= form_for @order, url: update_checkout_registration_path, method: :put, html: { id: 'checkout_form_registration' } do |f| %>
                  <p>
                    <%= f.label :email, Spree.t(:email) %><br />
                    <%= f.email_field :email, class: 'title form-control' %>
                  </p>
                  <p><%= f.submit Spree.t(:continue), class: 'btn btn-primary btn-block' %></p>
                <% end %>
              </div>
            </div>
          </div>
        </div>
        <% end %>
    </div>
  </div>
</div>